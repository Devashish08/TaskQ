# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build appropriations
*.test
*-test
*.out
/taskq-server # Or whatever your binary name is in cmd/taskq-server

# Output of the go coverage tool, specifically when used with LiteIDE
*.coverprofile

# Dependency directories (should be handled by go mod)
vendor/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db